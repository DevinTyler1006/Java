public class BasicJavaTest {
    public static void main(String[] args) {
    BasicJava someNumber = new BasicJava();

    // someNumber.print1To255();

    // someNumber.printOdd1To255();

    // someNumber.printSum();

    // int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // someNumber.iterateArray(testArr);

    // int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // someNumber.findMax(testArr);

    // int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // someNumber.getAverage(testArr);

    // someNumber.arrayOddNumbers();

    // int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // someNumber.greaterThanY(testArr, 5);

    // int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // someNumber.squareTheValues(testArr);

    // int[] testArr = {-1, 2, 3, -4, 5, -6, -7, 8, -9};
    // someNumber.eliminateNegativeNumbers(testArr);

    // int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    // someNumber.minMaxAvg(testArr);

    int[] testArr = {1, 2, 3, 4, 5, 6, 7, 8, 9};
    someNumber.shiftingValues(testArr);
    }
}